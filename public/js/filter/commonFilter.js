angular.module("johayo.filter",[]).filter('limitAndJjum',function(limitToFilter){return function(input,limit){if(input){if(input.length>limit){return limitToFilter(input,limit-3)+'...'}return input;}return input;}}).filter('cutHtmlTagAndLimit',function(limitToFilter){return function(input,limit){if(input){var changeInput=input.replace(/(<([^>]+)>)/ig,"");if(changeInput.length>limit){return limitToFilter(changeInput,limit-3)+'...'}return changeInput;}return input;};}).filter('getAttrSrc',['$sce',function($sce){return function(input,type){if(input){var tag='<div>'+input+'<div>';if(type=='img'){return $sce.trustAsResourceUrl($(tag).find('img:first').attr('src'));}else{return $sce.trustAsResourceUrl($(tag).find('iframe:first').attr('src'));}}return input;};}]).filter('changeTrustAdHtml',['$sce',function($sce){return function(input){if(input){return $sce.trustAsHtml(input);}return input;};}]).filter('firstCharUpper',function(){return function(input){if(input){return input.substring(0,1).toUpperCase()+input.substring(1,input.length);;}return input;};});